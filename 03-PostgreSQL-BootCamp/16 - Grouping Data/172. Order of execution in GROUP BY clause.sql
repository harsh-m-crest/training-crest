-- 172. Order of execution in GROUP BY clause
-- ##########################################

-- PostgreSQL evauates the GROUP BY clause after the FROM and where clause and before --
-- the having select, Distinct, ORDER BY and LIMIT clauses.


--
FROM
WHERE
GROUP BY
HAVING
SELECT
DISTINCT
ORDER BY
LIMIT

